-- ============================================================================
--  Имя кабинета рядом с его id. Выполнить после 013_account_owners.sql.
--  Скрипт идемпотентный.
-- ============================================================================
--
--  Кабинет теперь можно закрепить ДО того, как по нему пошёл расход: баер
--  вводит id, ключ Meta проверяет, что видит такой кабинет, и возвращает имя.
--  Имя хранится здесь — иначе каждое открытие страницы ходило бы в Meta ради
--  подписи, а строка без имени читалась бы как набор цифр.
alter table public.wh_account_owners add column if not exists account_name text;
